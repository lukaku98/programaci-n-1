#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char palabra1[15];
    char palabra2[15];
    char buffer[1024];
    int i;
    int cant;

    printf("ingrese una palabra: ");
    fflush(stdin);
    gets(buffer);
    while(stlen(buffer>14))
    {

    }

    cant=strlen(palabra1);

    strcpy(palabra2,palabra1);
    printf("cantidad de caracteres en palabra1: %d/n");
    printf("palabra1: %s \n palabra2: %s",palabra1, palabra2 );



    return 0;
}
